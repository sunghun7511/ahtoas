#ifndef __PARSER__
#define __PARSER__

#include "includes.hpp"
#include "IRCode.hpp"

class Parser {
private:
    
public:

};

void generate_code(const std::shared_ptr<IRCode> ircode, const std::ostream ostream);

#endif